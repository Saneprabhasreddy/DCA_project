services:
  mongo:
    image: mongo:7
    container_name: smartdca-mongo
    ports:
      - "${MONGO_PORT:-27017}:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - smartdca-net

  backend:
    build: ./backend
    container_name: smartdca-backend
    ports:
      - "${BACKEND_PORT:-5000}:5000"
    env_file:
      - .env
    environment:
      - PORT=${BACKEND_PORT:-5000}
    volumes:
      - ./data:/app/data:ro
    depends_on:
      - mongo
    networks:
      - smartdca-net

  ml-service:
    build: ./ml-service
    container_name: smartdca-ml-service
    ports:
      - "${FLASK_PORT:-5001}:5001"
    env_file:
      - .env
    environment:
      - FLASK_PORT=${FLASK_PORT:-5001}
    volumes:
      - ml_artifacts:/app/artifacts
    depends_on:
      - mongo
    networks:
      - smartdca-net

  frontend:
    build: ./frontend
    container_name: smartdca-frontend
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    env_file:
      - .env
    depends_on:
      - backend
    networks:
      - smartdca-net

volumes:
  mongo_data:
  ml_artifacts:

networks:
  smartdca-net:
    driver: bridge
